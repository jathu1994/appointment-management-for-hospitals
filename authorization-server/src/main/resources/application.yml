server:
  port: 8762
  
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ authorizationappointmentproject?createDatabaseIfNotExist=true
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialization-mode: never
    
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    
    
    
logging:
  level: 
    '[org.hibernate]': trace
